{
  "name": "@monky/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --import tsx/esm test/validators.test.ts 2>nul || node dist/test/validators.test.js 2>nul || tsc && node -e \"require('./test/validators.test.ts')\" 2>nul || tsc test/validators.test.ts --outDir dist/test && node dist/test/test/validators.test.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
